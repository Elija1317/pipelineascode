pipeline{
    agent any
    options{
        skipDefaultCheckout()
        timestamps()
        }
    stages{
        stage('Build'){
            agent any
                options{
                    skipDefaultCheckout()
                    timestamps()
                    }
          
            steps{
              echo "Pipline Build is done!"
              }
          
    }    
  }
}
